<div class="small-12 small-centered columns">
  <div class="row">
    <div class="small-8 columns">
      <div class="login-box">
        <h3>Edit your account</h3>

        <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
        <%= devise_error_messages! %>
        
        <div class="row collapse prefix-radius">
          <div class="small-1 columns">
            <span class="prefix"><i class="fi-mail"></i></span>
          </div>
          <div class="small-11 columns">
            <%= f.email_field :email, autofocus: true, placeholder: "Email" %>
          </div>
        </div>

        <div class="row collapse prefix-radius">
          <div class="small-1 columns">
            <span class="prefix"><i class="fi-torso"></i></span>
          </div>
          <div class="small-11 columns">
            <%= f.text_field :nickname, placeholder: "Nickname" %>
          </div>
        </div>

        <div class="row collapse prefix-radius">
          <div class="small-1 columns">
            <span class="prefix"><i class="fi-lock"></i></span>
          </div>
          <div class="small-11 columns">
            <%= f.password_field :password, autocomplete: "off", placeholder: "password (minimum 8 characters)" %>
          </div>
        </div>

        <div class="row collapse prefix-radius">
          <div class="small-1 columns">
            <span class="prefix"><i class="fi-lock"></i></span>
          </div>
          <div class="small-11 columns">
            <%= f.password_field :password_confirmation, autocomplete: "off", placeholder: "password confirmation" %>
          </div>
        </div>

        <div class="row collapse prefix-radius">
          <div class="small-1 columns">
            <span class="prefix"><i class="fi-lock"></i></span>
          </div>
          <div class="small-11 columns">
            <%= f.password_field :current_password, autocomplete: "off", placeholder: "Current password" %>
          </div>
        </div>

        <div class="row collapse">
          <%= f.submit "Edit your account" , :class => "button"%>
        </div>

        <% end %>
      </div>
    </div>
    <div class="small-4 columns">
      <div class="login-box">
        <h3>Cancel my account</h3>
        <p>You can completely delete your account if you desire to. All your personnal information will be deleted and any active support 
          towards jessy will be stopped. The comments you posted either on the blog or on the supporters pages will, however, <b>not</b> be deleted.</p>
          <a href="#" data-reveal-id="confirm-modal" class="button">Cancel your account</a>
          <div id="confirm-modal" class="reveal-modal" data-reveal aria-labelledby="modal-title" aria-hidden="true" role="dialog">
            <h2 id="modal-title">Confirmation</h2>
            <p>Are you sure you want to delete your account? When you go down that path, there is no coming back.</p>
            <%= button_to "Cancel my account", registration_path(resource_name), method: :delete, class: "button alert" %>
            <a class="close-reveal-modal" aria-label="Close">&#215;</a>
          </div> 
          <%= link_to "Back", :back %>
        </div>
      </div>
    </div>
  </div>